# React Chrome Extension using Excom

To begin testing this sample:

```shell
yarn build
```

or 
```shell
npm run build
```

This will create a dist folder containing the unpacked extension. You can then point your browser to load the dist folder.